# Full-grid delivery — 21 September 2026

The local dashboard at <http://127.0.0.1:3001> now contains all 11 constructors.
Nine new published references add McLaren, Red Bull, Aston Martin, Alpine,
Williams, Haas, Racing Bulls, Audi and Cadillac. Ferrari and Mercedes retain
all eight existing published releases; the complete archive has 17 releases.

The new models use Madrid track photographs dated 11 September and supplementary
paddock photographs dated 10 September, retrieved and inspected on 21 September.
They are estimated exterior reconstructions with simplified paintwork. Nose,
sidepod and engine-cover surfaces differ for every constructor. Other assemblies
share authored construction where photographs cannot establish exact sections,
hidden surfaces or suspension hardpoints. No factory accuracy or aerodynamic
performance is claimed. Williams uses its dated white Madrid livery; Haas' first
white-sidepod draft was rejected and corrected before publication.

Reference selection, image URLs and hashes are in
[`grid-2026-09-21.json`](../data/references/grid-2026-09-21.json).
Four-view assessments and frozen builder/catalog/GLB hashes are in
[`grid-reviews-2026-09-21.json`](../data/references/grid-reviews-2026-09-21.json).
Editable scenes and renders remain in `data/releases/grid-2026-6-reviewed/`;
the accepted Haas build is `haas-corrected/`. Published copies also reside in
the Docker release volume. Each new car has a GLB, editable Blender scene,
four 1080p previews and four 4K renders.

## Verification

- 137 backend tests and 14 archive-tool tests passed.
- Full-grid Blender geometry smoke checks passed, including unique nose,
  sidepod and engine-cover surfaces and surface-attached suspension.
  The final Haas correction changed paint only; its shape hashes are unchanged.
- Production Next.js and static builds passed.
- All 15 static browser tests passed, covering routes, comparisons, evidence,
  constructor selection and checksums for all exported assets.
- The live API reports 17 published releases and no unfinished jobs.
- The weekly worker and scheduler are running. The first full-grid intake
  imported 33 documents and 81 draft candidates, with 15 explicit source errors.
  See [weekly operation and review gates](weekly-updates.md).

## Static export

The complete validated bundle is:

`data/pages-export/2026-09-21-full-grid/pages-archive.tar.gz`

It contains 153 unique assets and 803,473,629 bytes of unpacked archive content.
Its SHA-256 is:

`22b91c2da8ae8ec7c18111aa272bb01b814eb6c351019086414364856aa0429a`

The static preview is built in `frontend/out`. Run `npm run preview:pages` to
serve it. `data/pages/snapshot.json` selects the GitHub Release
`pages-2026-09-21-full-grid`. The changes are delivered on `dev`; pushing that
branch does not deploy the public site. The hosted weekly workflow also requires
reaching the repository's default branch (`main`) before its schedule runs.
