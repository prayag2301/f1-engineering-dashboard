FROM node:20-alpine

WORKDIR /app

# Install dependencies at build time.
# node_modules is preserved via an anonymous volume in docker-compose,
# so the host directory does not override it.
COPY apps/web/package*.json ./
RUN npm install

EXPOSE 3000

CMD ["npm", "run", "dev"]
